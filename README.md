# 🔒 Secure Desktop

Легкий Docker-образ на базе **Alpine Linux** с графическим рабочим столом, доступным через браузер.

## ✨ Возможности

- 🐧 **Alpine Linux** — минимальный размер образа
- 🖥️ **XFCE Desktop** — легкое и быстрое окружение рабочего стола
- 🌐 **Доступ через браузер** — никаких VNC-клиентов, просто открой `http://localhost:3000`
- 📱 **Telegram Desktop** — официальный клиент
- 💬 **WhatsApp Web** — через Chromium в режиме приложения
- 🔐 **WireGuard VPN** — встроенная поддержка, автозапуск при старте
- 📁 **Доступ к файлам** — загрузки Telegram и WhatsApp доступны напрямую с хоста

## 🚀 Быстрый старт

### 1. Клонируй репозиторий

```bash
git clone https://github.com/katitusi/secure-desktop.git
cd secure-desktop
```

### 2. (Опционально) Настрой VPN

Создай папку `wireguard` и положи туда файл конфигурации:

```bash
mkdir wireguard
# Скопируй свой wg0.conf в папку wireguard/
```

### 3. Запусти

```bash
docker-compose up -d --build
```

### 4. Открой в браузере

Перейди на: **http://localhost:3000**

## 📁 Структура папок

```
secure-desktop/
├── Dockerfile              # Сборка образа
├── docker-compose.yml      # Конфигурация запуска
├── README.md               # Эта инструкция
│
├── config/                 # Настройки приложений (создаётся автоматически)
├── wireguard/              # Конфиг VPN (положи сюда wg0.conf)
│
└── downloads/              # 📥 ФАЙЛЫ МЕССЕНДЖЕРОВ (легкий доступ!)
    ├── telegram/           # Фото, видео, документы из Telegram
    └── whatsapp/           # Загрузки из WhatsApp Web
```

## 📥 Где искать файлы?

| Мессенджер | Папка на хосте | Что там |
|------------|----------------|---------|
| **Telegram** | `./downloads/telegram/` | Фото, видео, голосовые, документы |
| **WhatsApp** | `./downloads/whatsapp/` | Все загрузки из WhatsApp Web |

> 💡 **Совет:** Эти папки можно открыть в проводнике Windows напрямую!

## 🔧 Настройка

### Изменить разрешение экрана

В `docker-compose.yml` измени переменную:
```yaml
environment:
  - RESOLUTION=1920x1080
```

Доступные разрешения: `1280x720`, `1920x1080`, `2560x1440`

### Включить VPN вручную

Если VPN не стартует автоматически, открой терминал на рабочем столе и введи:
```bash
wg-quick up wg0
```

### Проверить статус VPN

```bash
wg show
```

### Остановить VPN

```bash
wg-quick down wg0
```

## 🛡️ Безопасность

- ✅ Весь трафик Telegram и WhatsApp идёт через VPN (если включен)
- ✅ Данные сохраняются локально в папке `config/`
- ✅ Контейнер изолирован от основной системы
- ⚠️ **Не коммить папки `config/`, `wireguard/`, `downloads/`** — они в `.gitignore`

## 🔄 Полезные команды

```bash
# Запустить
docker-compose up -d

# Остановить
docker-compose down

# Пересобрать (после изменений в Dockerfile)
docker-compose up -d --build

# Посмотреть логи
docker-compose logs -f

# Зайти в контейнер через терминал
docker exec -it secure-desktop bash
```

## 📝 Лицензия

MIT License
